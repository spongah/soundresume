<%= form_for(@artist, html: {class: "form-horizontal", style: "padding-left: 25px"}) do |f| %>
  <% if @artist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@artist.errors.count, "error") %> prohibited this artist from being saved:</h2>

      <ul>
      <% @artist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="form-group">
    <%= f.label :name, class: "control-label" %><br>
    <div class="col-sm-6">
      <%= f.text_field :name, class: "form-group" %>
    </div>
  </div>
    <div class="form-group">
    <%= f.label :genre, class: "control-label" %><br>
    <div class="col-sm-4">
      <%= f.collection_select :genre, Genre.all.order(:name), :name, :name, {prompt: false}, {class: "form-control"} %>
    </div>
  </div>

  <div class="form-group">
    <div class="col-sm-6">
      <%= f.submit class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
