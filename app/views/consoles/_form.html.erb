<%= form_for(@console, html: {class: "form-horizontal", style: "padding-left: 25px"}) do |f| %>
  <% if @console.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@console.errors.count, "error") %> prohibited this console from being saved:</h2>

      <ul>
      <% @console.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name, class: "control-label" %><br>
    <div class="col-sm-6">
      <%= f.text_field :name, class: "form-group" %>
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-6">
      <%= f.submit class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
